package com.example.instagram_clone_

import android.os.Bundle
import android.widget.Button
import android.widget.EditText
import androidx.appcompat.app.AppCompatActivity
import androidx.fragment.app.Fragment
import com.google.firebase.auth.FirebaseAuth
import com.google.firebase.auth.ktx.auth
import com.google.firebase.ktx.Firebase

class Login_Page_Fragement : Fragment(R.layout.activity_login_page) {
        /*val Email_or_phone_num : EditText=findViewById(R.id.email_or_phone)
        val password:EditText=findViewById(R.id.password)
        val login_btn:Button=findViewById(R.id.btn_login)
        val E_or_P:String= Email_or_phone_num.text.toString().trim()//email or phone store varialbe
        val _password:String=password.text.toString().trim()//password store variable
        login_btn.setOnClickListener()
        {

        }
        val sign_up:Button=findViewById(R.id.btn_create_account)
        sign_up.setOnClickListener()
        {

        }

        lateinit var auth: FirebaseAuth
        auth=Firebase.auth*/

}